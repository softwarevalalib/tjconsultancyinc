/* ============================================================
   Supabase browser configuration
   ============================================================
   Set these two values after creating the Supabase project. The publishable
   (or legacy anon) key is intended for browser use; never put a service_role
   key in this file or in any other front-end asset.
*/
(function (global) {
  'use strict';

  global.FMS_SUPABASE_CONFIG = {
    url: '',
    publishableKey: ''
  };
})(window);
